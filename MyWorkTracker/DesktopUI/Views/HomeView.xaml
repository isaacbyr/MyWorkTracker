<UserControl x:Class="DesktopUI.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopUI.Views"
             mc:Ignorable="d" Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border CornerRadius="18" Background="White">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"></ColumnDefinition>
                <ColumnDefinition Width="50"></ColumnDefinition>
                <ColumnDefinition Width="50"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="50"></ColumnDefinition>
                <ColumnDefinition Width="50"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="75">

                </RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <TextBlock Foreground="#ffaf7a" Text="My Work Tracker" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>

            <StackPanel Grid.Row="1" Grid.Column="0" Background="#ffaf7a">
                <TextBlock Text="Recents" Foreground="White"
                       FontSize="26"
                       Height="30"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Margin="0 10 0 5"></TextBlock>

                <RadioButton FontSize="16" Height="50" 
                         Content="Sat Nov 11" 
                                            
                         Style="{StaticResource MenuButtonTheme}">

                </RadioButton>
                <RadioButton FontSize="16" Height="50"
                         Content="Fri Nov 10"
                         Style="{StaticResource MenuButtonTheme}">

                </RadioButton>
            </StackPanel>
            
            <Button Foreground="White" Background="#ffaf7a" x:Name="New" Grid.Column="1" Grid.Row="0" Content="New"></Button>
            <Button Foreground="White" Background="#ffaf7a" x:Name="Search" Grid.Column="2" Grid.Row="0" Content="Search"></Button>
            <Button Foreground="White"  Background="#ffaf7a" x:Name="Logout" Grid.Column="4" Grid.Row="0" Content="Logout"></Button>
            <Button Foreground="White"  Background="#ffaf7a" x:Name="Exit" Grid.Column="5" Grid.Row="0" Content="Exit"></Button>
            
            <TextBlock 
            Grid.Row="0"
            Grid.Column="3" 
            x:Name="CurrDate"
            FontSize="24"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Foreground="#ffaf7a"
            FontWeight="Bold"></TextBlock>

        </Grid>

    </Border>
</UserControl>
